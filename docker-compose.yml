services:
  reportservice:
    build: .
    ports: ["8080:8080"]
    environment:
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__Default=Data Source=/data/reports.db
    volumes: ["report_data:/data"]
volumes:
  report_data:
